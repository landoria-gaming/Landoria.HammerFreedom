name: Release package
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  package:
    uses: landoria-gaming/LandoriaModActions/.github/workflows/release.yml@v4.4
    with:
      project-file: Landoria.HammerFreedom.csproj
      mod-name: Landoria.HammerFreedom
    secrets:
      references-token: ${{ secrets.MOD_REFERENCES_TOKEN }}
